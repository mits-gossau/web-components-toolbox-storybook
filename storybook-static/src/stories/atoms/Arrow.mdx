{/* Arrow.mdx */}

import { Canvas, Meta, Primary, Controls, Story, Source } from '@storybook/blocks';
import * as ArrowStories from './Arrow.stories';

<Meta of={ArrowStories} />

# Arrow

The arrow component displays an arrow in a chevron style. 

## Attributes / Values

<table>
	<thead>
		<tr>
			<th>attribute</th>
			<th>value</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>`direction`</td>
			<td>`up`, `right`, `down`, `left`</td>
		</tr>
		<tr>
			<td>`move`</td>
			<td><em>~ no value ~</em></td>
		</tr>
		<tr>
			<td>`hover-on-parent-shadow-root-host`</td>
			<td><em>~ no value ~</em></td>
		</tr>
	</tbody>
</table>

## Code

```html
<a-arrow />
```

```html
<a-arrow direction="up" />
```

```html
<a-arrow direction="right" />
```

```html
<a-arrow direction="down" />
```

```html
<a-arrow direction="left" />
```

```html
<a-arrow move direction="down" />
```

```html
<a-arrow move hover-on-parent-shadow-root-host direction="right" />
```

```html
<a-arrow move hover-on-parent-shadow-root-host direction="down" />
```

## Examples

<Story of={ArrowStories.Default} />
